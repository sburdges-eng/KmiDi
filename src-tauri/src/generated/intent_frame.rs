// AUTO-GENERATED by scripts/sync_entities.py. Do not edit manually.
use serde::{Deserialize, Serialize};
use super::emotion::{EmotionState, EmotionTag};

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub enum SectionRole {
    #[serde(rename = "intro")]
    Intro,
    #[serde(rename = "verse")]
    Verse,
    #[serde(rename = "chorus")]
    Chorus,
    #[serde(rename = "bridge")]
    Bridge,
    #[serde(rename = "outro")]
    Outro,
    #[serde(rename = "build")]
    Build,
    #[serde(rename = "drop")]
    Drop,
    #[serde(rename = "")]
    Unspecified,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct DSPTargets {
    #[serde(default)]
    pub filter_cutoff: f64,
    #[serde(default)]
    pub filter_cutoff_confidence: f64,
    #[serde(default)]
    pub reverb_send: f64,
    #[serde(default)]
    pub reverb_send_confidence: f64,
    #[serde(default)]
    pub drive: f64,
    #[serde(default)]
    pub drive_confidence: f64,
    #[serde(default)]
    pub stale: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct IntentConstraints {
    #[serde(default)]
    pub allowed_engines_mask: i64,
    #[serde(default)]
    pub forbidden_engines_mask: i64,
    #[serde(default)]
    pub max_cpu_cost: f64,
    #[serde(default)]
    pub max_event_rate: f64,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct IntentMeta {
    #[serde(default)]
    pub schema_version: i64,
    #[serde(default)]
    pub intent_id: i64,
    #[serde(default)]
    pub session_id: i64,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct IntentProvenance {
    #[serde(default)]
    pub source: i64,
    #[serde(default)]
    pub user_override_weight: f64,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct MusicHints {
    #[serde(default)]
    pub key: String,
    #[serde(default)]
    pub tempo_bpm: f64,
    #[serde(default)]
    pub chord_bias: String,
    #[serde(default)]
    pub section_role: SectionRole,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct MusicalIntent {
    #[serde(default)]
    pub tempo_bias: f64,
    #[serde(default)]
    pub rhythmic_density: f64,
    #[serde(default)]
    pub groove_strength: f64,
    #[serde(default)]
    pub harmonic_tension: f64,
    #[serde(default)]
    pub harmonic_motion: f64,
    #[serde(default)]
    pub mode_preference: i64,
    #[serde(default)]
    pub melodic_activity: f64,
    #[serde(default)]
    pub contour_variance: f64,
    #[serde(default)]
    pub dynamic_range: f64,
    #[serde(default)]
    pub texture_density: f64,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct TimeScope {
    #[serde(default)]
    pub start_bar: i64,
    #[serde(default)]
    pub end_bar: i64,
    #[serde(default)]
    pub fade_in_beats: f64,
    #[serde(default)]
    pub fade_out_beats: f64,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct IntentFrame {
    #[serde(default)]
    pub meta: IntentMeta,
    #[serde(default)]
    pub timestamp_ms: i64,
    #[serde(default)]
    pub emotion: EmotionState,
    #[serde(default)]
    pub music: MusicalIntent,
    #[serde(default)]
    pub music_hints: MusicHints,
    #[serde(default)]
    pub dsp_targets: DSPTargets,
    #[serde(default)]
    pub time: TimeScope,
    #[serde(default)]
    pub constraints: IntentConstraints,
    #[serde(default)]
    pub provenance: IntentProvenance,
    #[serde(default)]
    pub latency_budget_ms: f64,
}

impl IntentFrame {
    pub fn validate(&self) -> Result<(), String> {
        if self.meta.schema_version != 1 {
            return Err(format!("unsupported schema_version {}", self.meta.schema_version));
        }
        if self.music.tempo_bias < -1.0 || self.music.tempo_bias > 1.0 {
            return Err(format!("tempo_bias {} out of range [-1.0, 1.0]", self.music.tempo_bias));
        }
        if self.provenance.source < 0 || self.provenance.source > 5 {
            return Err(format!("source {} out of range [0, 5]", self.provenance.source));
        }
        if self.time.end_bar != -1 && self.time.start_bar != -1 && self.time.end_bar <= self.time.start_bar {
            return Err(format!("end_bar {} must be > start_bar {}", self.time.end_bar, self.time.start_bar));
        }
        Ok(())
    }
}
