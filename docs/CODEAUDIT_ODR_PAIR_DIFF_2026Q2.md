# ODR Pair Diff Report — harmony / osc / groove Consolidation

Generated by `scripts/audit/odr_pair_diff.sh`. Per-file comparison of the
14 duplicate .cpp files between `src/` and `src_penta-core/`. Both sides
compile into separate static libs and both link into `bindings/penta_core_native`,
creating a real ODR violation per `CODEAUDIT_CXX_DEEP_2026Q2_FOLLOWUP.md §1`.

The audit prescribed "src_penta-core/ canonical, delete src/ duplicates" —
but actual code shows some src/ files have **strictly more methods** than
their penta-core counterparts. Deleting those would break linkage (verified
experimentally on 2026-04-13: `penta::harmony::VoiceLeading::voiceProgression(vector<Chord>, ...)`
is called from `KellyCore/ChordGenerator.cpp` and only defined in `src/`).

**Per-pair recommended canonical side below.** `src/only` methods that have
external callers are flagged explicitly — those MUST be preserved.

---

## harmony/ChordAnalyzer.cpp

| side | methods | size |
|------|---------|------|
| `src/` | 10 | 11731 B |
| `src_penta-core/` | 10 | 8691 B |

**Recommended canonical**: **MANUAL REVIEW** — APIs match but bodies differ (293 vs 196 lines; src_penta-core/ (mtime newer))

### In both (10)

- `ChordAnalyzer::analyze`
- `ChordAnalyzer::analyzeSIMD`
- `ChordAnalyzer::ChordAnalyzer`
- `ChordAnalyzer::findBestMatch`
- `ChordAnalyzer::findBestMatchSIMD`
- `ChordAnalyzer::scoreAgainstTemplate`
- `ChordAnalyzer::scoreAgainstTemplateSIMD`
- `ChordAnalyzer::setConfidenceThreshold`
- `ChordAnalyzer::setTemporalSmoothing`
- `ChordAnalyzer::update`

---

## harmony/ChordAnalyzerSIMD.cpp

| side | methods | size |
|------|---------|------|
| `src/` | 3 | 5433 B |
| `src_penta-core/` | 3 | 5438 B |

**Recommended canonical**: **MANUAL REVIEW** — APIs match but bodies differ (170 vs 170 lines; src_penta-core/ (mtime newer))

### In both (3)

- `ChordAnalyzer::analyzeSIMD`
- `ChordAnalyzer::findBestMatchSIMD`
- `ChordAnalyzer::scoreAgainstTemplateSIMD`

---

## harmony/HarmonyEngine.cpp

| side | methods | size |
|------|---------|------|
| `src/` | 9 | 4760 B |
| `src_penta-core/` | 9 | 5018 B |

**Recommended canonical**: **MANUAL REVIEW** — APIs match but bodies differ (149 vs 160 lines; src/ (mtime newer))

### In both (9)

- `HarmonyEngine::~HarmonyEngine`
- `HarmonyEngine::getChordHistory`
- `HarmonyEngine::getScaleHistory`
- `HarmonyEngine::HarmonyEngine`
- `HarmonyEngine::processNotes`
- `HarmonyEngine::suggestVoiceLeading`
- `HarmonyEngine::updateChordAnalysis`
- `HarmonyEngine::updateConfig`
- `HarmonyEngine::updateScaleDetection`

---

## harmony/ScaleDetector.cpp

| side | methods | size |
|------|---------|------|
| `src/` | 7 | 4937 B |
| `src_penta-core/` | 7 | 4937 B |

**Recommended canonical**: **either (BYTE-IDENTICAL)** — auto-resolvable, pick either, delete other

### In both (7)

- `ScaleDetector::analyze`
- `ScaleDetector::correlateWithProfile`
- `ScaleDetector::findBestScale`
- `ScaleDetector::ScaleDetector`
- `ScaleDetector::setConfidenceThreshold`
- `ScaleDetector::setDecayFactor`
- `ScaleDetector::update`

---

## harmony/VoiceLeading.cpp

| side | methods | size |
|------|---------|------|
| `src/` | 10 | 10198 B |
| `src_penta-core/` | 6 | 6623 B |

**Recommended canonical**: src/

### Only in `src/harmony/VoiceLeading.cpp` (4)

- `VoiceLeading::analyze` (no external callers)
- `VoiceLeading::calculateSmoothness` (no external callers)
- `VoiceLeading::invertVoicing` (no external callers)
- `VoiceLeading::voiceProgression` (no external callers)

### In both (6)

- `VoiceLeading::calculateCost`
- `VoiceLeading::calculateMotionCost`
- `VoiceLeading::findOptimalVoicing`
- `VoiceLeading::generateVoicingCandidates`
- `VoiceLeading::updateConfig`
- `VoiceLeading::VoiceLeading`

---

## osc/OSCClient.cpp

| side | methods | size |
|------|---------|------|
| `src/` | 9 | 3600 B |
| `src_penta-core/` | 9 | 3779 B |

**Recommended canonical**: **MANUAL REVIEW** — APIs match but bodies differ (114 vs 132 lines; src_penta-core/ (mtime newer))

### In both (9)

- `OSCClient::~OSCClient`
- `OSCClient::connectInternal`
- `OSCClient::disconnectInternal`
- `OSCClient::OSCClient`
- `OSCClient::send`
- `OSCClient::sendFloat`
- `OSCClient::sendInt`
- `OSCClient::sendString`
- `OSCClient::setDestination`

---

## osc/OSCHub.cpp

| side | methods | size |
|------|---------|------|
| `src/` | 10 | 3401 B |
| `src_penta-core/` | 10 | 3878 B |

**Recommended canonical**: **MANUAL REVIEW** — APIs match but bodies differ (105 vs 134 lines; src/ (mtime newer))

### In both (10)

- `OSCHub::~OSCHub`
- `OSCHub::matchPattern`
- `OSCHub::OSCHub`
- `OSCHub::processCallbacks`
- `OSCHub::receiveMessage`
- `OSCHub::registerCallback`
- `OSCHub::sendMessage`
- `OSCHub::start`
- `OSCHub::stop`
- `OSCHub::updateConfig`

---

## osc/OSCMessage.cpp

| side | methods | size |
|------|---------|------|
| `src/` | 10 | 1988 B |
| `src_penta-core/` | 10 | 2027 B |

**Recommended canonical**: **MANUAL REVIEW** — APIs match but bodies differ (71 vs 73 lines; src_penta-core/ (mtime newer))

### In both (10)

- `OSCMessage::addBlob`
- `OSCMessage::addFloat`
- `OSCMessage::addInt`
- `OSCMessage::addString`
- `OSCMessage::getArgument`
- `OSCMessage::getBlob`
- `OSCMessage::getFloat`
- `OSCMessage::getInt`
- `OSCMessage::getString`
- `OSCMessage::OSCMessage`

---

## osc/OSCServer.cpp

| side | methods | size |
|------|---------|------|
| `src/` | 5 | 3406 B |
| `src_penta-core/` | 5 | 2978 B |

**Recommended canonical**: **MANUAL REVIEW** — APIs match but bodies differ (116 vs 104 lines; src_penta-core/ (mtime newer))

### In both (5)

- `OSCServer::~OSCServer`
- `OSCServer::getMessageQueue`
- `OSCServer::OSCServer`
- `OSCServer::start`
- `OSCServer::stop`

---

## osc/RTMessageQueue.cpp

| side | methods | size |
|------|---------|------|
| `src/` | 7 | 1488 B |
| `src_penta-core/` | 7 | 1422 B |

**Recommended canonical**: **MANUAL REVIEW** — APIs match but bodies differ (74 vs 64 lines; src_penta-core/ (mtime newer))

### In both (7)

- `RTMessageQueue::~RTMessageQueue`
- `RTMessageQueue::clear`
- `RTMessageQueue::isEmpty`
- `RTMessageQueue::pop`
- `RTMessageQueue::push`
- `RTMessageQueue::RTMessageQueue`
- `RTMessageQueue::size`

---

## groove/GrooveEngine.cpp

| side | methods | size |
|------|---------|------|
| `src/` | 10 | 11107 B |
| `src_penta-core/` | 10 | 11634 B |

**Recommended canonical**: **MANUAL REVIEW** — APIs match but bodies differ (334 vs 337 lines; src_penta-core/ (mtime newer))

### In both (10)

- `GrooveEngine::~GrooveEngine`
- `GrooveEngine::analyzeSwing`
- `GrooveEngine::applySwing`
- `GrooveEngine::detectTimeSignature`
- `GrooveEngine::GrooveEngine`
- `GrooveEngine::processAudio`
- `GrooveEngine::quantizeToGrid`
- `GrooveEngine::reset`
- `GrooveEngine::updateConfig`
- `GrooveEngine::updateTempoEstimate`

---

## groove/OnsetDetector.cpp

| side | methods | size |
|------|---------|------|
| `src/` | 7 | 4891 B |
| `src_penta-core/` | 7 | 6744 B |

**Recommended canonical**: **MANUAL REVIEW** — APIs match but bodies differ (171 vs 200 lines; src_penta-core/ (mtime newer))

### In both (7)

- `OnsetDetector::~OnsetDetector`
- `OnsetDetector::computeSpectralFlux`
- `OnsetDetector::detectPeaks`
- `OnsetDetector::OnsetDetector`
- `OnsetDetector::process`
- `OnsetDetector::reset`
- `OnsetDetector::setThreshold`

---

## groove/RhythmQuantizer.cpp

| side | methods | size |
|------|---------|------|
| `src/` | 6 | 4695 B |
| `src_penta-core/` | 6 | 5436 B |

**Recommended canonical**: **MANUAL REVIEW** — APIs match but bodies differ (137 vs 148 lines; src_penta-core/ (mtime newer))

### In both (6)

- `RhythmQuantizer::applySwing`
- `RhythmQuantizer::findNearestGridPoint`
- `RhythmQuantizer::getGridInterval`
- `RhythmQuantizer::quantize`
- `RhythmQuantizer::RhythmQuantizer`
- `RhythmQuantizer::updateConfig`

---

## groove/TempoEstimator.cpp

| side | methods | size |
|------|---------|------|
| `src/` | 8 | 4998 B |
| `src_penta-core/` | 8 | 5483 B |

**Recommended canonical**: **MANUAL REVIEW** — APIs match but bodies differ (162 vs 168 lines; src_penta-core/ (mtime newer))

### In both (8)

- `TempoEstimator::addOnset`
- `TempoEstimator::computeCorrelation`
- `TempoEstimator::estimateTempo`
- `TempoEstimator::findBestInterval`
- `TempoEstimator::getSamplesPerBeat`
- `TempoEstimator::reset`
- `TempoEstimator::TempoEstimator`
- `TempoEstimator::updateConfig`

---

## Consolidation plan (derived)

After reviewing the above, for each pair the action is one of:

1. **src/ canonical** → add `list(FILTER KELLY_CORE_SOURCES EXCLUDE REGEX)`
   for the `src_penta-core/` counterpart in `src_penta-core/CMakeLists.txt`
   (or delete the penta file). Ensure `penta_core` target still compiles
   without it (check its own internal callers).

2. **src_penta-core/ canonical** → add `list(FILTER KELLY_CORE_SOURCES EXCLUDE
   REGEX)` for the `src/` counterpart in the root `CMakeLists.txt`, and add
   `penta_core` as a `PUBLIC` link dep of `KellyCore`.

3. **Manual merge** → port missing methods from the non-canonical side into
   the canonical, verify byte-exact behaviour of shared methods, then proceed
   as (1) or (2).

4. **API-equivalent** → pick the side with better implementation quality
   (per `SALVAGE_CATALOG_2026Q2.md`, src_penta-core/ has more recent commits
   but this is per-file verified, not a blanket rule).

No file deletions performed by this report. Human review required before
CMakeLists.txt edits.

## Behavioral-equivalence caveats

The API-level analysis above only catches *link-correctness* issues. Pairs
that link cleanly may still differ in runtime behaviour (analysis cadence,
buffer layout, dedup logic, etc.). See
[`CODEAUDIT_ODR_BEHAVIORAL_CAVEATS_2026Q2.md`](CODEAUDIT_ODR_BEHAVIORAL_CAVEATS_2026Q2.md)
for the known algorithmic differences in `GrooveEngine`, `OnsetDetector`,
`HarmonyEngine`, and `OSCServer`, plus suggested spot-check procedures.
