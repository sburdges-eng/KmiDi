# Bridges

Phase 0 bridge scaffolding for external/runtime integrations.

Planned subdomains:

- `core/` for `minimal_listening_core` bridge wrappers.
- `legacy/` for KmiDi bridge compatibility wrappers.
- `python_ml/` for optional Python/iDAW ML assist wrappers.

All bridge domains are feature-flagged and disabled by default.
