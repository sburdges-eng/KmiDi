# apps/kmidi

Minimal placeholder for the KmiDi + iDAW app (see [DISCOVERY_WORKSPACE_SCAFFOLD_APPS_KMIDI.md](../../docs/DISCOVERY_WORKSPACE_SCAFFOLD_APPS_KMIDI.md)).

- **This directory:** `pyproject.toml` only; no Python package or config yet.
- **Reference layout:** `~/Dev/workspace-scaffold/apps/kmidi` has the full stub: `kmidi/` package (agents, tools), config (datasets.toml, training.yaml), ROADMAP.md, and tests. Use that as the reference when expanding this app or migrating the scaffold into the monorepo.
