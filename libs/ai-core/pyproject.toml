[project]
name = "ai-core"
version = "0.1.0"
description = "KmiDi ML and music brain core libraries"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.21",
    "torch>=2.0",
    "scipy>=1.8",
    "pyyaml>=6.0",
]
